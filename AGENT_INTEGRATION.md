# Agentic Workflow Integration

- OpenAPI-first
- Provide test API key
- Idempotency for writes
- Optional webhooks
- Example flow:
  - GET /sets
  - GET /sets/{id}/items
  - PUT /sets/{id}/items/{item_id}
